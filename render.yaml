databases:
  - name: cloud-deploy-db
    databaseName: cloud_deploy
    user: clouddeploy
    plan: starter # $7/month plan
    region: oregon # or your preferred region

services:
  - type: redis
    name: cloud-deploy-redis
    plan: starter # $10/month (or use 'free' for testing)
    region: oregon
    ipAllowList: [] # internal access only

  - type: web
    name: cloud-deploy-api
    runtime: docker
    plan: starter # $7/month plan
    region: oregon
    dockerfilePath: ./Dockerfile.complete
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cloud-deploy-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: cloud-deploy-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: RUN_MIGRATIONS
        value: "true"
      - key: PORT
        value: "8000"
      - key: FRONTEND_URL
        value: "*" # Change this to your frontend URL in production
